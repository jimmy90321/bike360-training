rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    //允許任何已認證用戶讀寫 riders 集合
    match /riders/{riderName}/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
